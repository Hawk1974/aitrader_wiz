# Templates

The current version renders Markdown and YAML from code. This folder reserves space for future externalized templates if the output format needs to be operator-editable without recompiling the wizard.
